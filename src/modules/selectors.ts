// Global DOM selectors
const body = document.querySelector('body')
const hamburger = document.getElementById('hamburger')
const navigationDots = document.querySelectorAll('#scrolling-navigation .dot')
const mobileNavItems = document.querySelectorAll('#mobile-nav a')
const projectTags = document.querySelectorAll('.work-header .josh-cta')
const webAndMusicProjects = document.querySelectorAll('.work-content')

export {
  body,
  hamburger,
  navigationDots,
  mobileNavItems,
  projectTags,
  webAndMusicProjects,
}
