---
export interface Props {
  text: string
  url: string
  class?: string
}

const { text, url, class: className } = Astro.props
---

<a class={`${className} josh-cta`} href={url}>{text}</a>

<style>
  a {
    padding: 1rem;
    font-size: var(--size-small);
  }

  @media (min-width: 768px) {
    a {
      font-size: var(--size-medium);
    }
  }
</style>
