---
import SectionHeader from '../Global/SectionHeader.astro'
import Card from '../Global/Card.astro'
---

<section id="services">
  <SectionHeader text="Services" />
  <div class="service-cards">
    <Card class="card">
      <span>01</span>
      <h3>Web Design & Development</h3>
      <p>
        I create custom web designs that engage your audience and bring your
        creative vision to life.
      </p>
      <p>
        By using modern web development practices, your site will benefit from
        clean code, fast page loads and great SEO by default.
      </p>
      <p>
        Maintainance, feature add-ons, upgrades - the sky's the limit when
        creative minds come together.
      </p>
    </Card>
    <Card class="card">
      <span>02</span>
      <h3>Currently Using</h3>
      <ul>
        <li>HTML5</li>
        <li>CSS3</li>
        <li>JavaScript / TypeScript</li>
        <li>React</li>
        <li>NextJS</li>
        <li>Sanity CMS</li>
        <li>Figma</li>
      </ul>
    </Card>
  </div>
</section>

<style>
  h2 {
    margin-bottom: 2.5rem;
  }

  @media (min-width: 768px) {
    h2 {
      margin-bottom: 4rem;
      text-align: center;
    }
  }

  .service-cards {
    display: grid;
    grid-template-columns: 1fr;
  }

  @media (min-width: 768px) {
    .service-cards {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .card {
    width: 100%;
  }

  @media (min-width: 768px) {
    .card {
      padding-top: 2rem;
      padding-bottom: 2rem;
      border-bottom: none;
      text-align: center;
    }
  }

  .card:first-of-type {
    padding-bottom: 2rem;
    border-bottom: 1px solid var(--color-light-grey);
  }

  @media (min-width: 768px) {
    .card:first-of-type {
      padding-right: 2rem;
      border-bottom: none;
    }
  }

  .card:last-of-type {
    padding-top: 2rem;
  }

  @media (min-width: 768px) {
    .card:last-of-type {
      padding-left: 2rem;
      border-left: 1px solid var(--color-light-grey);
    }
  }

  .card span {
    display: inline-block;
    margin-bottom: 1.5rem;
    font-size: var(--size-large);
    color: var(--color-green);
    text-shadow: 0 0 1px var(--color-purple);
  }

  @media (min-width: 768px) {
    .card span {
      margin-bottom: 2.5rem;
      font-size: var(--size-extra-large);
    }
  }

  .card h3 {
    margin-bottom: 1rem;
    font-size: var(--size-medium);
  }

  @media (min-width: 768px) {
    .card h3 {
      margin-bottom: 1.5rem;
      font-size: var(--size-large);
    }
  }

  @media (min-width: 768px) {
    .card p {
      width: 100%;
      max-width: 500px;
      margin: auto;
    }
  }

  .card p,
  .card li {
    font-size: var(--size-extra-small);
  }

  @media (min-width: 768px) {
    .card p,
    .card li {
      font-size: var(--size-small);
    }
  }

  .card p:not(:last-child),
  .card li:not(:last-child) {
    margin-bottom: 0.5rem;
  }

  @media (min-width: 768px) {
    .card p:not(:last-child),
    .card li:not(:last-child) {
      margin-bottom: 1rem;
    }
  }
</style>
